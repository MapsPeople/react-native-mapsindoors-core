import MPLocation from "./MPLocation";
/**
 * Base type of a {@link MPLocation}.
 *
 * @export
 * @enum {number}
 */
export enum MPLocationType {
    room,
    poi,
    building,
    venue,
    area,
    asset,
    floor
}
/**
 * A container for a height/width pair.
 *
 * @export
 * @interface MPSize
 * @typedef {MPSize}
 */
export default interface MPSize {
    /**
     * Height.
     *
     * @type {number}
     */
    height: number;
    /**
     * Width.
     *
     * @type {number}
     */
    width: number;
}